#include<bits/stdc++.h>

using namespace std;

int main()
{
    string one, two, result;

    cin >> one >> two;

    result = one == two ? one : "1";

    cout << result << endl;

    return 0;
}
