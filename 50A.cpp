#include<bits/stdc++.h>

using namespace std;

int main()
{
    int n, m;

    cin >> n >> m;

    //cout << (long long) floor(n / 2) * (long long) m << endl;
    cout << n * m / 2 << endl;

    return 0;
}

